# Kora brand assets

- `kora-icon-source.png` is the original uploaded artwork.
- `kora-icon-master.png` is the square, high-resolution master used to derive the app icons in `app/`.

Keep generated favicon and platform-sized files in `app/` so Next.js can discover them through its metadata file conventions.
